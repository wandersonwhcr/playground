{
  "$id": "playground/games",
  "type": "object",
  "required": ["_id", "name"],
  "additionalProperties": false,
  "properties": {
    "_id": {
      "$id": "#_id",
      "type": "string",
      "format": "uuid"
    },
    "name": {
      "$id": "#name",
      "type": "string",
      "minLength": 1,
      "maxLength": 100
    }
  }
}
